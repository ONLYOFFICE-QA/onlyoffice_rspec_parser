require:
  - rubocop-performance
  - rubocop-rspec

inherit_from:
  - .rubocop_todo.yml

AllCops:
  NewCops: enable

RSpec/RepeatedExample:
  Exclude:
    - 'spec/spec_examples/duplicates/*'

RSpec/RepeatedDescription:
  Exclude:
    - 'spec/spec_examples/duplicates/one_dublicates_spec.rb'
